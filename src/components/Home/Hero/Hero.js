import { Typography } from "antd";
import React from "react";

const Hero = () => {
  const { Title } = Typography;
  return (
    <div>
      <Title level={2}>This is Hero Section</Title>
    </div>
  );
};

export default Hero;
